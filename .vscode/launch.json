{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Current File with Root",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}